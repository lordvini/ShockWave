using UnityEngine;
using System.Collections;

public class FXMyMenuComponent : MonoBehaviour 
{

	/*public static IEnumerator FXElements(float timeCoroutine, float timeAnimation, GameObject target, string TypeAnimation)
	{
		yield return WaitForSeconds (timeCoroutine);

		switch (TypeAnimation)
		{
			case MenuGlobalReference.instance.FXTypeFade:
				iTween.FadeTo(target, iTween.Hash("alpha", 1, "time", timeAnimation, "oncompletetarget", gameObject, "oncomplete", "FadeGround"));
			break;

			case MenuGlobalReference.instance.FXTypeMove:
			break;

			case MenuGlobalReference.instance.FXTypeScale:
			break;
		}
	}*/
}
